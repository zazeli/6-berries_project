{
  "name": "6-berries",
  "version": "1.0.0",
  "description": "Simple design to take a look into display: flex proterties",
  "main": "index.js",
  "scripts": {
    "dev": "live-server --port=3000 --host=localhost",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "html15",
    "css3",
    "flex"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}